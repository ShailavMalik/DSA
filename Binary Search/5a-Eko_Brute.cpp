// Brute force approach
#include <iostream>
using namespace std;
int height_setting(int arr[], int n, int l)
{
    
}

int main()
{
    int N, M;
    cin >> N >> M;
    int arr[N];
    for (int i = 0; i < N; i++)
    {
        cin >> arr[i];
    }
    cout << height_Setting(arr, N, M);

    return 0;
}